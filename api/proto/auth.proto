syntax = "proto3";

package api;

option go_package = "api/proto";

service Auth {
  rpc Auth (AuthRequest) returns (AuthResponse){}
}

message AuthRequest{
  string login = 1;
  string password = 2;
}

message AuthResponse {
  string res = 1;
}